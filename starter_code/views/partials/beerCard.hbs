<div class="partial-beer-container">
    <img src={{this.image_url}} alt="beer-pic">
    <div class="beer-body">
        <h1>{{this.name}}</h1>
        <h2>{{this.tagline}}</h2>
        <p>{{this.description}}</p>
    </div>
</div>